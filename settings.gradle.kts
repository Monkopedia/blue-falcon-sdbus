rootProject.name = "blue-falcon-sdbus"

pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}

include(":engine")
include(":integration-tests")
